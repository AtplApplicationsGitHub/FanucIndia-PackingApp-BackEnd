BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[SalesOrder] ADD CONSTRAINT [SalesOrder_status_df] DEFAULT 'R105' FOR [status];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
